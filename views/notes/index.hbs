{{toolId}}

<h1>All Notes</h1>
{{#each toolId}}
<div class="row">
    <div class="col s16 m6">
        <div class="card blue-grey darken-1">
            <div class="card-content white-text">
                <span class="card-title">{{createdAt}}</span>
                <p>{{noteContent}}</p>
            </div>
            <div>
                <a href="{{toolUser}}/edit"><button class="btn">Update</button></a>
                <form action="/{{../toolUser}}?_method=DELETE" method="POST">
                    <button class="btn" type="submit">Delete</button>
                </form>
            </div>
        </div>
    </div>
</div>